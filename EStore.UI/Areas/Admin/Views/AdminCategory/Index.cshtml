@model List<ResultCategory>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row justify-content-center">
        <div class="col-md-12 text-center">
            <div class="bg-secondary rounded p-4">
                <h6 class="mb-4 text-center">Categories Edit Page</h6>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Category Name</th>
                            <th>Category Href Url</th>
                            <th>Category Icon</th>
                            <th>Update</th>
                            <th>Remove</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>@count</td>
                                <td>@item.CategoryName</td>
                                <td>@item.CategoryHrefUrl</td>
                                <td>@item.CategoryIcon</td>
                                <td><a class="btn btn-info btn-sm" href="/Admin/AdminCategory/UpdateCategory/@item.Id">Update</a></td>
                                <td><a class="btn btn-danger btn-sm" href="/Admin/AdminCategory/RemoveCategory/@item.Id">Remove</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                    <div class="text-center">
                        <a href="/Admin/AdminCategory/CreateCategory/" class="btn btn-success btn-sm">Create Category</a>
                    </div>
            </div>
        </div>
    </div>
</div>


