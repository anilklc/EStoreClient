@model List<ResultSliderImage>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row justify-content-center">
        <div class="col-md-12 text-center">
            <div class="bg-secondary rounded p-4">
                <h6 class="mb-4 text-center">Slider Images Edit Page</h6>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Slider Title</th>
                            <th>Slider Image Path</th>
                            <th>Update</th>
                            <th>Remove</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>@count</td>
                                <td>@item.SliderTitle</td>
                                <td><img src="@item.ImagePath" width="50" height="50" style="margin-right:10px" /></td>
                                <td><a class="btn btn-info btn-sm" href="/Admin/AdminSliderImage/UpdateSliderImage/@item.Id">Update</a></td>
                                <td><a class="btn btn-danger btn-sm" href="/Admin/AdminSliderImage/RemoveSliderImage/@item.Id">Remove</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="text-center">
                    <a href="/Admin/AdminSlider/Index/" class="btn btn-success btn-sm">Go Back Sliders</a>
                    @if (count == 0)
                    {
                        <a href="/Admin/AdminSliderImage/CreateSliderImage/" class="btn btn-success btn-sm">Create Slider Image</a>
                    }

                </div>
            </div>
        </div>
    </div>
</div>

