@model List<ResultReviewImage>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row justify-content-center">
        <div class="col-md-12 text-center">
            <div class="bg-secondary rounded p-4">
                <h6 class="mb-4 text-center">Review Images Edit Page</h6>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Reviewer Name</th
                            <th>Review Title</th>
                            <th>Review Image Path</th>
                            <th>Update</th>
                            <th>Remove</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>@count</td>
                                <td>@item.ReviewerName</td>
                                <td>@item.ReviewTitle</td>
                                <td><img src="@item.ImagePath" width="50" height="50" style="margin-right:10px" /></td>
                                <td><a class="btn btn-info btn-sm" href="/Admin/AdminReviewImage/UpdateReviewImage/@item.Id">Update</a></td>
                                <td><a class="btn btn-danger btn-sm" href="/Admin/AdminReviewImage/RemoveReviewImage/@item.Id">Remove</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="text-center">
                    <a href="/Admin/AdminReview/Index/" class="btn btn-success btn-sm">Go Back Reviews</a>
                    @if (count == 0)
                    {
                        <a href="/Admin/AdminReviewImage/CreateReviewImage/" class="btn btn-success btn-sm">Create Review Image</a>
                    }

                </div>
            </div>
        </div>
    </div>
</div>

